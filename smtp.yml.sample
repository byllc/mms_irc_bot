development:
  from:    'test@testing.com'
  address: smtp.gmail.com
  port:   587
  enable_starttls_auto:  true
  user_name: username
  password:  password
  authentication: !ruby/symbol plain # :plain, :login, :cram_md5, no auth by default
  domain:         test.domain.tld # the HELO domain provided by the client to the server
